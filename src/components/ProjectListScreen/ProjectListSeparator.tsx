import React from "react";
import { StyleSheet, View } from "react-native";

const ProjectListSeparator = () => {
  return <View style={styles.container} />;
};

const styles = StyleSheet.create({
  container: {
    height: 15,
  },
});

export default ProjectListSeparator;
